---
import cn from "@/libs/cn"
const props = Astro.props;
---

<input  class={cn("w-full rounded-md  bg-background px-3 py-2 text-sm text-secondary-foreground outline-none ring-[1px] ring-primary focus:ring-2 focus:ring-primary disabled:cursor-not-allowed disabled:opacity-50", props.class)} {...props}>
